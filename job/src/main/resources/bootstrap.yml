server:
  port: ${SERVER_PORT:8999}
spring:
  profiles:
    include: core,dev
  application:
    name: job
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 192.168.30.86:8848
        file-extension: yaml
        prefix: job
        username: 'nacos'
        password: 'nacos'
      discovery:
        enabled: true
        server-addr: 192.168.30.86:8848
